@model IEnumerable<MRT_Demo.Models.Stategy>

@{
    ViewBag.Title = "Recycle Bin";
}

<h2>กล่องขยะยุทธศาสตร์</h2>
<div class="row">
    <div class="col-md-2">
        @Html.ActionLink("ย้อนกลับ", "Index", "Stategies",new {id = Model.First().StrategicObjectiveID} , new { @class = "btn btn-secondary" })
    </div>
    <div class="col-md-6">
    </div>
    <div class="col-md-4">
    </div>
</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UpdateDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stategy1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UpdateDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stategy1)
        </td>
        <td>
            @Html.ActionLink("คืนค่า", "Revert", new { id =item.ID },new { @class = "btn" }) |
            @Html.ActionLink("ลบ", "LastDelete", new { id =item.ID }, new { @class = "btn" })
        </td>
    </tr>
}

</table>


<script>
    function ToStrategicObjectives(id, a) {
        $("#SEOform").attr("action", '@Url.Action("Index","StrategicObjectives")');
        $("#SEOform").submit();
    }
</script>
